const assert = require('assert');
const DrinkerService = require('../main/Service/DrinkerService')

describe('ServiceTests', () => {
    describe('DrinkerServiceTest', function () {
        describe('DrinkerSignUp', function () {
            
        });
    });
    it('should return 2', () => {
        assert.equal(1 + 1, 2);
    });
    it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });
});